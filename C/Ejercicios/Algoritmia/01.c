#include<stdio.h>
#include<stdlib.h>
/*
Algoritmo en si mismo
vampiro(numero_original, numeros_restantes, conjunto_s)
    Si al multiplicar los elementos del conjunto_s == numero_original
        retornar verdadero
    Si es par y la mitad del largo del conjunto_s == cantidad de cifras del numero original
        retornar falso
    Si es impar y 
    else
        





numeros_restantes = numero_original
si tiene un numero par de digitos:
    ejecutar vampiro(numero_original, numeros_restantes, conjunto_s)

*/

int main(void){
    return 0;
}
