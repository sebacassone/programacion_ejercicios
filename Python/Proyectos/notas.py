# Bloque de importaciones
import tkinter as tk
from tkinter import messagebox

# Bloque de definiciones

# Bloque de funciones
# Sugerencia antes que se me olvide, agregar un boton mejor para
# generar espacio para agregar notas


# Bloque principal
# Se crea venteana
ventana = tk.Tk()

# Se crea la equiqueta de la primera nota
nota_1_etiqueta = tk.Label(ventana, text="Nota 1")
# Se crea la entrada de la primera nota
nota_1 = tk.Entry(ventana)
# Se crea etiqueta para ponderado
ponderado_1_etiqueta = tk.Label(ventana, text="%")
# Se crea la entrada para el ponderado
ponderado_1 = tk.Entry(ventana)
# Se crea etiqueta para titulo de nota final
nota_final_etiqueta = tk.Label(ventana, text="Nota final")
# Se crea entrada para nota final
nota_final = tk.Label(ventana, text="")
# Se crea boton para agregar más notas

# Se crea el ponderador de cada nota


# Se crea boton para crear más notas.
agregar_notas = tk.Button(ventana, )

# Se crea titulos para programa


# Se ponen en pantalla

ventana.mainloop()
